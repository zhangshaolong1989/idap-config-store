server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://idap-eureka:${server.port}/eureka
    register-with-eureka: false
    fetch-registry: false

idap:
  test:
    value: 123adafdsasafd
